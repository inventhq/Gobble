# Secrets for the tracker platform.
# In production, populate via: kubectl create secret generic tracker-secrets --from-env-file=.env -n tracker
# Or use sealed-secrets / external-secrets-operator for GitOps.
#
# This file is a TEMPLATE — do NOT commit real values.
apiVersion: v1
kind: Secret
metadata:
  name: tracker-secrets
  namespace: tracker
type: Opaque
stringData:
  HMAC_SECRET: "CHANGE_ME"
  PLATFORM_API_KEY: "CHANGE_ME"
  RISINGWAVE_URL: "CHANGE_ME"
  R2_ENDPOINT: "CHANGE_ME"
  R2_ACCESS_KEY_ID: "CHANGE_ME"
  R2_SECRET_ACCESS_KEY: "CHANGE_ME"
  LANCEDB_API_KEY: "CHANGE_ME"
  # Optional
  SLM_API_KEY: ""
  ADMIN_API_KEY: ""
