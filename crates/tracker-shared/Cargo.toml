[package]
name = "tracker-shared"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v7"] }
axum = "0.8"
tokio = { version = "1", features = ["net", "rt"] }
tracing = "0.1"
